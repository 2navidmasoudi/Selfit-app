import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    mainContainer: {flex: 0},
    mainRowWrapper: {flex: 1, flexDirection: 'row'},
    wrapper: {flex: 1},
});