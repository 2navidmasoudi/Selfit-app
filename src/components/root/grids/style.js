import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mainContainer: { flex: 1, backgroundColor: '#0F9D7A' },
  mainRowWrapper: { flex: 1, flexDirection: 'row' },
  wrapper: { flex: 1 },
});
export { styles };
